services:
  frontend:
    build: ./frontend/my-judge-frontend
    ports:
      - "5173:5173"
    networks:
      - mern

  backend:
    build: ./backend
    ports:
      - "8081:8081"
    networks:
      - mern
    env_file:
      - ./backend/.env

networks:
  mern:
    driver: bridge
